import React from 'react';

import classes from './ViewEditModal.module.css';
import Card from './Card';

const ViewEditModal = (props) => {
  // const showData;

  // if(props.postID === 'View'+props.postID){
  //   props.postItem.map(post => {

  //   })
  // }
  return (
    <Card>

    </Card>
  );
}

export default ViewEditModal;